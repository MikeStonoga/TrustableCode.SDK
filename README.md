# TrustableCode.SDK

This repository is now organized by major SDK iteration.

- `v1/` preserves the current SDK implementation.
- `v2/` is the next version, aligned more directly with the discipline described in *Trustable Code*.

The active implementation plan is kept in:

- `v2/docs/implementation-plan.md`

